あなたは、複数日にわたる対象メールをまとめて要約し、Obsidianの各日のデイリーノートおよび企業ノートに追記するバックフィルタスクを行います。以下の手順に厳密に従ってください。確認は不要です、そのまま実行してください。

1. このプロンプトの末尾に記載されている「対象ファイル一覧」の各JSONファイルをReadツールで直接読み込む(Bashやスクリプト実行は使わず、テキストとしてそのまま読めばよい)。各ファイルは1日分のデータで、次の形式を持つ:
   - `target_date`: その日の日付(YYYY-MM-DD、JST)
   - `emails`: 対象メールの配列。各要素は `from_address`, `from_name`, `subject`, `date_jst`, `body`, `rule_type`("job"/"company"), `rule_comment`, `base_tag`(付与すべきタグの基準) を持つ。
   - `count` が 0 のファイルはスキップしてよい。

2. 読み込んだファイル(日付)ごとに、以下を行う。

   a. 対象デイリーノートのパスを特定する: `{{OBSIDIAN_VAULT_DIR}}/Diary/{YYYY}/{MM}/{YYYY-MM-DD}.md`
      - ファイルが存在しない場合は `{{OBSIDIAN_VAULT_DIR}}/Daily/Daily_Template.md` の内容をコピーして新規作成してから進める。
      - ファイルは存在するが `### Mails` 見出しが無い場合は、`### Memo` 見出しの直前に `### Mails` 見出しを追加する(テンプレートの並び順: Meals → Mails → Memo に合わせる)。

   b. ノートの `### Mails` セクションを確認し、その日のメールについて**既に同内容の要約が記載済みでないか**を送信元・件名(・企業名)で照合する。既に記載済みのものはスキップする(重複追記しない)。`### Memo` セクションに過去の実行で誤って記載されたメール要約が残っている場合は、それも「記載済み」とみなしてスキップしてよい(この場合は移動や削除はせず、そのまま残しておく)。

   c. `rule_type` が `"job"` でまだ記載されていないメールについて、以下を行う。
      - メール本文から、実際に企業の説明や選考(エントリー開始、ES締切、説明会、面接など)に関する具体的な情報が書かれている企業を抽出する。スカウトサービスの定型文・広告文のみで実質的な情報が無いものは対象外。
      - 該当する企業ごとに:
        - デイリーノートに載せる1行以内の要約を作る。例:「○○社が本選考ES募集開始」。
        - `{{OBSIDIAN_VAULT_DIR}}/Companies/` 内にその企業のノートを探す(表記ゆれがあれば近い名前の既存ファイルを流用する)。既存ノートがあれば末尾に `- {メール受信日 YYYY-MM-DD}: {選考・企業情報の要約}` を追記する。無ければ `Companies/{企業名}.md` を新規作成し、同様に追記する。いずれの場合も既存の内容は変更・削除しない。
      - メール本文にイベントや記事の紹介が含まれている場合は、それらの概要を1件あたり1〜2行程度で要約する(企業の選考情報とは別項目として扱う)。これは企業ノートには書かず、デイリーノートのみに記載する。
      - URL・リンクは付与しない。

   d. `rule_type` が `"company"` でまだ記載されていないメールについて、以下を行う(このメールは基本的に1社の採用担当から届く。`rule_comment` は「選考開始時期・企業情報・働き方などまとめ」)。
      - メール本文・`from_name` から企業名を特定する(部署名・チーム名の接尾辞は除く。例:「サンプル株式会社　採用プロジェクトチーム」→「サンプル株式会社」)。
      - メール本文に選考情報(エントリー開始、ES締切、説明会、選考フローの案内など)が含まれる場合、デイリーノートに載せる1行以内の要約を作る。例:「○○社が本選考ES募集開始」。含まれない場合はデイリーノートへの追記は無し(その場合でも下記の企業ノート更新は必ず行う)。
      - 選考情報の有無に関わらず、必ず以下を行う:
        - `{{OBSIDIAN_VAULT_DIR}}/Companies/` 内にその企業のノートを探す(表記ゆれがあれば近い名前の既存ファイルを流用する)。
        - 既存ノートが無ければ `{{OBSIDIAN_VAULT_DIR}}/Companies/Companies_Template.md` の見出し構成(基本情報/事業概要/業績/働く環境/新卒採用の各項目)をそのまま使って `Companies/{企業名}.md` を新規作成する。
        - 既存ノートがあり、かつテンプレートと同じ見出し構成を持つ場合は、メール本文から読み取れる情報で**空欄の項目のみ**を埋める。既に記入済みの項目は上書き・削除しない。
        - 既存ノートがテンプレートと異なる構成(自由記述のメモなど)の場合は、既存内容は変更せず、ファイル末尾にテンプレートの見出し構成で新しいセクションを追加し、わかる項目を埋める。
        - メール本文から読み取れない項目は空欄のままにしてよい(無理に埋めない、推測で書かない)。
      - URL・リンクは付与しない。

   e. 手順c・dで作った全項目を、以下の形式の箇条書きにする(時刻は付けない):
      `- [送信者名] 要約文 #タグ`
      例: `- [OpenWork 就活情報] OpenWorkからDYM（人材・研修・WEB事業）の新卒採用エントリー受付開始案内。「世界で一番社会を変える会社を創る」ビジョンを掲げ、コンサルティング・人材紹介に加え医療事業にも展開。 #Career`
      - タグは、そのメールの `base_tag` を基本として付与する。`base_tag` が空でない場合は必ず含める。企業・選考系の項目は `base_tag` が無ければ `#Career` を基本とする。内容から見て他に該当するタグがあれば追加してよい(1〜2個程度まで)。適切な既存タグが無ければ新しいタグを付けてよい。
      - 手順c・dで企業ごとに作った項目は、まとめて1つの要約にせず、企業ごとに個別の箇条書きにする。

   f. その日の全項目を、おおまかなトピックごとにグループ化し、近い話題が連続するように並び替える。

   g. 作成した箇条書きを、その日のデイリーノートの `### Mails` セクション末尾(次の見出し `### Memo` の直前)に追記する。`### Memo` セクションやその他の既存の内容は一切変更・削除しないこと。メール要約を `### Memo` に書いてはいけない。

3. 全ての日付の処理が終わったら、日付ごとに「追記件数」「スキップ件数(重複)」を一覧で報告し、更新・新規作成した企業ノートの一覧(company タイプについては埋めた項目名も)もあわせて報告する。
