あなたは、指定した送信元から今日届いたメールを要約し、Obsidianのデイリーノートおよび企業ノートに追記するタスクを行います。以下の手順に厳密に従ってください。確認は不要です、そのまま実行してください。

1. このプロンプトの末尾に記載されている「対象ファイル」のパスのJSONファイルをReadツールで直接読み込む(Bashやスクリプト実行は使わず、テキストとしてそのまま読めばよい)。
   - `target_date`: 対象日(YYYY-MM-DD、JST)
   - `emails`: 対象メールの配列。各要素は `from_address`, `from_name`, `subject`, `date_jst`, `body`, `rule_type`("job"/"company"), `rule_comment`(処理方針の元コメント), `base_tag`(付与すべきタグの基準) を持つ。
   - `count` が 0 の場合は何もせず終了してよい。

2. 対象デイリーノートのパスを特定する: `{{OBSIDIAN_VAULT_DIR}}/Diary/{YYYY}/{MM}/{YYYY-MM-DD}.md` (YYYY, MM, DDは target_date から算出)。
   - ファイルが存在しない場合は `{{OBSIDIAN_VAULT_DIR}}/Daily/Daily_Template.md` の内容をコピーして新規作成してから進める。
   - ファイルは存在するが `### Mails` 見出しが無い場合は、`### Memo` 見出しの直前に `### Mails` 見出しを追加する(テンプレートの並び順: Meals → Mails → Memo に合わせる)。

3. `rule_type` が `"job"` のメールについて、以下を行う(対象アドレスの `rule_comment` は「企業、選考、イベント、記事があればデイリーノートに記録。」)。
   a. メール本文から、実際に企業の説明や選考(エントリー開始、ES締切、説明会、面接など)に関する具体的な情報が書かれている企業を抽出する。スカウトサービスの定型文・広告文のみで実質的な情報が無いものは対象外。
   b. 該当する企業ごとに:
      - デイリーノートに載せる1行以内の要約を作る。例:「○○社が本選考ES募集開始」。
      - `{{OBSIDIAN_VAULT_DIR}}/Companies/` 内にその企業のノートを探す(表記ゆれがあれば近い名前の既存ファイルを流用する)。既存ノートがあれば末尾に `- {YYYY-MM-DD}: {選考・企業情報の要約}` を追記する。無ければ `Companies/{企業名}.md` を新規作成し、同様に追記する。いずれの場合も既存の内容は変更・削除しない。
   c. メール本文にイベントや記事の紹介が含まれている場合は、それらの概要を1件あたり1〜2行程度で要約する(企業の選考情報とは別項目として扱う)。これは企業ノートには書かず、デイリーノートのみに記載する。
   d. URL・リンクは付与しない。

4. `rule_type` が `"company"` のメールについて、以下を行う(このメールは基本的に1社の採用担当から届く。対象アドレスの `rule_comment` は「選考開始時期・企業情報・働き方などまとめ」)。
   a. メール本文・`from_name` から企業名を特定する(部署名・チーム名の接尾辞は除く。例:「サンプル株式会社　採用プロジェクトチーム」→「サンプル株式会社」)。
   b. メール本文に選考情報(エントリー開始、ES締切、説明会、選考フローの案内など)が含まれる場合、デイリーノートに載せる1行以内の要約を作る。例:「○○社が本選考ES募集開始」。含まれない場合はデイリーノートへの追記は無し(この場合でも c は必ず行う)。
   c. 選考情報の有無に関わらず、必ず以下を行う:
      - `{{OBSIDIAN_VAULT_DIR}}/Companies/` 内にその企業のノートを探す(表記ゆれがあれば近い名前の既存ファイルを流用する)。
      - 既存ノートが無ければ `{{OBSIDIAN_VAULT_DIR}}/Companies/Companies_Template.md` の見出し構成(基本情報/事業概要/業績/働く環境/新卒採用の各項目)をそのまま使って `Companies/{企業名}.md` を新規作成する。
      - 既存ノートがあり、かつテンプレートと同じ見出し構成を持つ場合は、メール本文から読み取れる情報で**空欄の項目のみ**を埋める。既に記入済みの項目は上書き・削除しない。
      - 既存ノートがテンプレートと異なる構成(自由記述のメモなど)の場合は、既存内容は変更せず、ファイル末尾にテンプレートの見出し構成で新しいセクションを追加し、わかる項目を埋める。
      - メール本文から読み取れない項目は空欄のままにしてよい(無理に埋めない、推測で書かない)。
   d. URL・リンクは付与しない。

5. 全ての要約項目(手順3b・3c・4bで作った内容)を、以下の形式の箇条書きとして作成する(時刻は付けない):
   `- [送信者名] 要約文 #タグ`
   例: `- [OpenWork 就活情報] OpenWorkからDYM（人材・研修・WEB事業）の新卒採用エントリー受付開始案内。「世界で一番社会を変える会社を創る」ビジョンを掲げ、コンサルティング・人材紹介に加え医療事業にも展開。 #Career`
   - タグは、そのメールの `base_tag` を基本として付与する。`base_tag` が空でない場合は必ず含める。企業・選考系の項目は `base_tag` が無ければ `#Career` を基本とする。内容から見て他に該当するタグがあれば追加してよい(1〜2個程度まで)。適切な既存タグが無ければ新しいタグを付けてよい。
   - 手順3bで企業ごとに作った項目は、まとめて1つのメール要約にせず、企業ごとに個別の箇条書きにする。

6. 全項目を、おおまかなトピックごとにグループ化し、近い話題が連続するように並び替えたうえで、対象デイリーノートの `### Mails` セクション末尾(次の見出し `### Memo` の直前)に追記する。`### Memo` セクションやその他の既存の内容は一切変更・削除しないこと。メール要約を `### Memo` に書いてはいけない。

7. 最後に、デイリーノートへの追記件数と、更新・新規作成した企業ノートの一覧(company タイプについては埋めた項目名も)を報告する。
